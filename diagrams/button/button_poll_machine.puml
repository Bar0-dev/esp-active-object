@startuml

[*] --> POLLING : BUTTON_POLL_SIG
POLLING : if (currState != prevState)\n{arm debounce timer, disarm poll timer}
POLLING --> DEBOUNCED : BUTTON_DEBOUCE_SIG
DEBOUNCED --> [*] : currState != prevState \n publish BUTTON_STATE_CHANGED_SIG
DEBOUNCED : arm poll timer

@endum